# Verification — September 11, 2026

## Automated checks

- `npm run check`: no errors or warnings.
- `npm run lint`: Prettier and ESLint pass.
- `npm run test:unit -- --run`: 70 tests across sixteen files pass, including browser interactions.
- `npm run build`: static production build succeeds.
- Numerical tests cover regression, overfitting, nonlinear networks, chronological splits, sorting, k-means convergence, Q-learning, and actual adaptation of word probabilities.
- Inference tests cover fragmented SSE, multimodal payloads, tool calls, retrieval ranking/projection, source grounding, and explicit evaluation criteria.
- Network tests verify the complete architecture, every incoming contribution, activation arithmetic, input changes, prediction traces, and linear regression outputs.
- Token tests verify actual `o200k_base` IDs, exact text reconstruction, Unicode byte fragments, and image partitions that cover every source pixel exactly once.

## Live OpenAI checks

Used the supplied local `.env` connection with `gpt-4.1-mini`; no key value was displayed or stored in test artifacts.

- Language: a three-sentence answer generated and streamed successfully; API usage reported 43 input and 45 output tokens.
- Agent: five model turns selected four real tool calls (read sales, summarize weekdays, summarize weekends, calculate). The final answer correctly reported 140 weekday items, 245 weekend items, and a difference of 105.
- Vision OCR: invitation title, Saturday 18 October, doors 18:30, and tour 19:00 were correct. The model acknowledged the missing location.
- Vision spatial questions: cup, fern, invitation, and their relative positions were correctly described.
- Vision comparison: distinguished the garden/dog/ball/bench scene from the coast/sailboat/lighthouse scene.
- Vision upload: an actual WebP uploaded through the file chooser was correctly described as a green/beige sneaker and a brown leather boot.
- Vision cancellation recovered the controls; retry completed successfully.
- Retrieval: real 256-dimensional embeddings ranked the source library, and generation cited the ticket passage with the correct £12 adult price and free child admission. Editing the source to £15 and rebuilding embeddings changed the answer to £15. A parking question correctly received an answer that the notes do not provide the information.
- Evaluation: both brief and careful prompt styles scored 6/6 on the six explicit checks. This small test suite does not establish general reliability. Cancellation left the active case Stopped and the remainder Not run, and saved runs remained accessible.

## Visual review

Desktop and 390-pixel mobile layouts inspected in light and dark themes. All eighteen chapters are reachable; the sidebar scrolls independently and its footer does not overlap the final chapter. No mobile horizontal overflow observed. Original training/validation/test visibility was preserved. Image details support keyboard panning without switching chapters.

### Neural and token revision

- The default classifier renders all 27 nodes (2 inputs, two layers of 12 neurons, one output), all 180 weights, and 25 biases. Selecting hidden-layer neuron 12 exposes its twelve actual incoming contributions.
- A live browser run completed 600 training epochs and reached 100% accuracy on the 32 held-out synthetic test examples. This verifies this teaching experiment, not general model performance.
- The network was inspected on desktop and phone, in both themes. Changing an input changes the selected neuron's calculation. The complete graph can pan horizontally on small screens.
- The dedicated tokens chapter shows actual local tokenization, including spaces, multilingual text, and emoji byte fragments. Image mode supports 16, 64, and 144 patches, two source images, selection, magnification, separation, and reassembly.
- Both generated infographics were visually inspected and refined for correct arithmetic and counts: sixteen image patches correspond to sixteen illustrated embeddings. The artwork is conceptual; the live explorer uses exact source pixels.
- The full 1536 × 1024 footwear photograph preserves its aspect ratio. Desktop and phone checks confirmed that the boot shaft is visible, both silhouette controls work, and the phone page stays within its 390-pixel viewport.
- The local app returned HTTP 200 and `.env` remains ignored by Git. OpenAI request code was unchanged in this revision; the live API checks below were performed during the earlier expansion.

## Visual and interaction refinement — September 11

- Reviewed all seventeen chapters in both themes. Captured 102 final lab views across 1440-, 900-, and 390-pixel viewports; inspected key interactions separately and checked the overfitting layout at 700 pixels.
- Corrected phone clipping in the sorting stage and its action row, and tablet clipping of model choices and regularization controls. Narrow overfitting layouts now stack before the paired charts become cramped.
- Aligned chapter navigation, plot headings, action rows, form fields, figure controls, and footer navigation. Training metrics now sit beside their loss curves beneath the main plot; phone users can start training beside the visualization.
- Neural architecture changes preserve the mounted explorer and animate geometry. Selection is distinct from hover and keyboard focus. Inspectors keep a stable height, and incoming contributions form four desktop columns or two phone columns.
- Prediction traces support pause, resume, reset, and direct stage selection. Pausing leaves the trace unchanged; reduced-motion settings suppress animated geometry and reveal effects.
- The sorting scrubber supports exact forward/backward replay and pauses automatic playback. Browser tests assert that all six bars and every action fit on a phone.
- Polynomial curves ease between real model fits. The café inspector shows the selected guest's assigned center and actual distance. Delivery routes follow measured cell centers, and playback pauses and resumes at its current step.
- Text and agent composers appear before their results on phones. Settings retain their frame and action position when changing providers. Retrieval results and citations reveal their source passage; vision details center the image and focus its keyboard-scroll region.
- Transformer connectors meet their corresponding token centers; every stage remains available on phones. Image-patch selections reveal the matching sequence position without moving the page. Adaptation reports paused training accurately and preserves learning when reselecting an existing setting.
- Native reflection disclosures animate smoothly, including interrupted reversals, and retain the selected answer after closing. Browser tests cover these transitions. Chapter entry, mobile navigation, focus indicators, and illustration dialogs were also inspected.
- Svelte autofixer reports no issues or suggestions for every changed component. All 54 tests, type checks, lint, and the static build pass. No inference transport or API-key handling changed in this pass.

## Wide-screen composition and navigation — September 11

- Bounded the complete desktop frame to 1740 pixels and lesson content to 1360, with navigation kept beside the content on ultrawide displays. Reduced the header from 76 to 58 pixels. Removed sidebar numbers, completion marks, the active dot, the active pill, and redundant chapter icon rows.
- Introduced charcoal surfaces with sage, lavender, blue, and warm chapter accents; retained distinct light surfaces. Both sides of joined experiment panels have a visible, continuous rounded silhouette.
- Rebuilt the data-split lesson as an image-and-explanation composition. Its 1536 × 1024 image remains fully visible with its native 3:2 ratio at 390, 700, 900, 1920, and 2800 pixels in both themes. The footwear image also preserves its full ratio, with comparison controls alongside on desktop.
- Replaced the offset rectangle taxonomy with interactive translucent nested discs. Circle containment expresses AI → machine learning → deep learning → LLMs; selection and the separate generative/foundation/multimodal dimensions work with keyboard and pointer input.
- Removed the desktop illustration overlay. Narrow screens retain a small glass expansion icon. Reduced oversized training, language, and agent actions, compacted the agent's visible dataset, and aligned the evaluation controls with their heading.
- Capped the neural graph at 1040 pixels, tightened its controls and inspector, and made trace stages follow horizontally on phones without moving the page. All 27 nodes and 180 weights remain represented. Training plots and controls stack at 800 pixels before the chart becomes cramped.
- Captured 102 chapter views at 2560, 1440, and 390 pixels in both themes, plus targeted 2800/1920/900/700 checks. No document overflow. Inspected the full-image compositions, map selections, phone navigation, neural tracing, settings, training pause/resume, and reduced motion. Final agent and evaluation views were captured again after their spacing changes.
- All 54 tests across twelve files, Svelte check (zero errors/warnings), lint, component autofixer, and production build pass. The existing large lazy tokenizer chunk warning remains. No inference transport or API-key handling changed; `.env` is still ignored.

## Transparent artwork, unified workbench, and self-supervision — previous revision

- Replaced all nineteen chapter illustrations with a coherent set of transparent editorial sculptures. All selected originals and delivered WebP files have genuine alpha, checked on both `#141619` and `#f3f2ef`. Full-size artwork totals approximately 4 MB, with 800-pixel responsive variants. Existing scene photographs remain as real inputs to the vision/captioning labs.
- Images are contained at their full proportions; no subject is cropped. Generated type was replaced with native DM Sans labels. The token illustration was simplified to four patches and four vector capsules after rejecting a misleading bar-chart version and a flattened checkerboard edit. Final prompts are in `art-direction-v2.json`; dimensions and alpha measurements are in `art-assets-v2.json`.
- Removed exploration state/footer and the curiosity slogan. Reviewed Hugeicons at navigation size, including brain for generalization, a dedicated presentation easel, and a separate magnifier for images. Image expansion is a 34-pixel glass control on hover/focus and a persistent 44-pixel target on touch screens. Modal zoom, fit, close, presentation toggling, and Escape were exercised.
- Unified residual green panel backgrounds around blue-charcoal surfaces. Reduced image footprints and caption spacing, aligned labels to the neuron stages, and aligned the opening headline with the content grid.
- Rebuilt both neural labs as integrated workbenches. Inputs and current predictions stay with the sample map, actual network, and neuron inspector. Narrow screens use focused views sharing the same controls. All 27 nodes and 180 connections remain; ordinal labels are removed. Tests cover exact inspection arithmetic, training, trace controls, and phone graph containment.
- Added a real 144 → 24 → 2 → 24 → 144 autoencoder with MSE, backpropagation, and Adam. Its 108 training and 36 unseen procedural stroke images are distinct. Browser training through 300 passes reduced MSE from 0.2309 to 0.0040 on training images and from 0.2312 to 0.0056 on unseen images. Finite differences check gradients across all layers. Latent-map input, decoding, nearest examples, interpolation, pause, reset, and keyboard controls were exercised.
- Added a compact follow-on explanation connecting sensory experience, data-derived targets, and pretraining. Its three illustrative puzzles reveal the original reconstruction target, the exact missing image crop, or an observed next token; they never claim to run a model. All three modes were checked at 1440/390 in both themes, including keyboard reveal and resetting the reveal on mode changes.
- Captured 108 chapter/layout combinations: all eighteen chapters at 2560, 1440, and 390 pixels in both themes, with an opening and lab screenshot for each. No browser runtime errors or document overflow. Inspected artwork contact sheets in both themes, every widget composition, and full-size neural, footwear, token, self-supervision, and opening views. Additional checks cover full-aspect images at 2800/1440/700/390, touch expansion, and data-split controls.
- All changed Svelte components passed the autofixer. Type checking, lint, and 60 tests pass. Inference transport and API-key handling were not changed; the OpenAI checks recorded above are from the earlier live-model expansion. `.env` remains ignored.
- Rechecked 48 representative layouts after the final image sizing changes. Opening phone tabs are single-line 44-pixel controls; the classification subview uses the new transparent shoes. Direct chapter links leave room for the sticky header. All 38 delivered artwork files (nineteen full-size and nineteen responsive variants) preserve alpha, totaling approximately 5.2 MB.

## MNIST, integrated image typography, and compact artwork — previous pass

- Replaced the procedural stroke autoencoder with real 28 × 28 MNIST handwriting: 8,000 official training examples and 2,000 official test examples. Every bundled pixel and label was checked against the original CVDF IDX files. Dataset and checkpoint provenance are recorded in `static/data/MNIST-PROVENANCE.md`.
- A `784 → 96 → 32 → 2 → 32 → 96 → 784` autoencoder runs real gradient updates in a dedicated browser worker. The two bottleneck values are the plotted coordinates. All 2,000 held-out encodings are plotted, with a spatially thinned layer of real, colorized digit thumbnails. Labels never enter the worker or training objective. Geometry interpolates between actual updates without invented positions.
- Kept the original, reconstruction, coordinate controls, and nearest handwriting beside the map; phones use a compact inspector immediately above it. Selecting examples, coordinate probes, digit filters, morphing, learned/random starting weights, pause, and reset are functional. The empty learning-curve placeholder collapses until there are measured updates. Main-thread animation stayed responsive during training.
- Random-start browser training reduced held-out sample MSE from 0.2321 to 0.0993 after 17 updates, at approximately 52 ms per batch in the measured run. The explicitly labeled saved starting point has MSE 0.0436482 across all 2,000 held-out examples. Its reproducible training script loads only pixels. Training minimizes pixel cross-entropy; displayed errors are MSE on clearly identified fixed samples. A two-number bottleneck visibly loses detail; the UI explains this limitation.
- Verified automatic pause when the tab is hidden or the workbench leaves the viewport, worker teardown on unmount, corrupt/missing checkpoint fallback to random weights, and retry after a failed dataset fetch. There is no automatic training on page load.
- Generated eight new transparent illustrations with short labels composed into their ceramic objects using the built-in image model. Removed external HTML label rows. Checked all text and both theme composites; rejected edits with baked checkerboards. The final prompt set is in `docs/art-direction-v2.json` under `labeledRevision`.
- Selectively trimmed vertical alpha margins on eleven illustrations. Kept full horizontal compositions and padding around all meaningful subjects. The forecasting image is 495 pixels high instead of 941; the opening illustration is 633 instead of 941. Image width is capped independently so cropping actually saves page height. Updated intrinsic dimensions and artwork revision URLs. All 38 full/responsive WebP assets retain real alpha; combined size is 5,645,640 bytes.
- Reviewed 72 layout/theme combinations across the changed chapters at 2560, 1440, and 390 pixels. The final MNIST workbench was additionally inspected at 1920, 1440, 700, and 390 pixels in both themes. No document overflow or browser runtime errors. Touch expansion uses a visible 44 × 44 control; zoom, fit, and close passed in both themes.
- All changed Svelte components passed the autofixer with zero issues or suggestions. The final checks cover mathematical gradients, serialization, true-coordinate thumbnail selection, actual MNIST loading/training, and UI interaction. Type checking, lint, and the static production build pass; the full suite has 61 passing tests. A production-preview smoke test also loaded the bundled worker and trained from random weights, reaching 0.0926 held-out sample MSE after 17 updates with no runtime errors. OpenAI transport and key handling were not changed, and `.env` remains ignored.

## Curated artwork, Fashion-MNIST, and GPU latent map — current pass

- Generated sixty-five candidates: five distinct concepts for each of thirteen replacement illustrations. Six user selections are applied exactly; the other seven are documented in `art-review-v3.md`. The six requested retained images are unchanged. All candidates remain available at `/art-review/index.html` with prompts, selection marks, enlargement, and a theme switch.
- Selected images have real transparency, compact alpha crops, and full/800-pixel WebP exports. Both background composites were inspected. Rejected diagrams include malformed neuron arithmetic, incorrect attention layouts, ambiguous image patch correspondence, and unequal pixels marked equal. Captions and alt text now describe the actual selections.
- Replaced the deeper-network curve-fitting example with a real Fashion-MNIST ConvNet: three 3 × 3 convolutions with 8, 16, and 24 feature maps, followed by a ten-class head. All 8,578 parameters are learned. Every displayed map and kernel is calculated from those weights; selecting a cell shows its receptive field. Muting a feature recalculates predictions, and Find a mistake selects an actual held-out error.
- The ConvNet checkpoint used 10,000 official training images. A disjoint 2,000 validation images selected epoch 34 of 35 by validation loss. It scores 88.0% on validation and 87.05% on 2,000 official test images. Browser training updates every layer and evaluates those same full held-out sets. The production build loaded the compiled worker, trained 100 steps from random weights to 69.6% test accuracy on WebGPU, and restored the original checkpoint and metrics. PyTorch and browser inference agree within 9 × 10⁻⁶ in logits.
- Rebuilt the MNIST autoencoder as a variational autoencoder with GELU hidden activations and a linear reconstruction head: 784 → 128 → 32 → 2 → 32 → 128 → 784. Training uses a stochastic two-dimensional code, reconstruction MSE, a KL term scaled by 1/784, and Adam. The map plots posterior means; labels never enter the training worker. The checkpoint was trained for 10,000 updates on training images only and has full-test MSE 0.0424393.
- Fused JaxJS updates run on WebGPU or compiled WASM. On the measured Apple Metal adapter, batches took 6.4–7.1 ms, compared with approximately 52 ms in the previous JavaScript implementation. Two hundred updates plus five full test snapshots took 1.85 seconds. WASM took 3.5–3.8 ms per batch for this small model. This is a local benchmark, not a cross-device performance guarantee.
- All 2,000 test glyphs appear at their actual mean codes. Axes stay fixed; there is no PCA, label-driven arrangement, or invented jitter. Pointer capture supports continuous probing, with immediate local decoding and keyboard control. The inspection values and displayed pixels match the CPU reference. Training maintained 61 animation frames per second in the measured browser run; the map no longer jumps because of changing axis bounds.
- Production-preview checks exercised both bundled GPU workers, actual training, feature ablation, finding a mistake, checkpoint restoration, and continuous latent probing. MNIST random-start test MSE fell from 0.1136 to 0.0501 after 200 updates. No page errors or failed network requests occurred. Evidence is in `test-results/production-models-v3.json` and `test-results/mnist-gpu-upgrade/benchmark.json`.
- Captured 84 final layout/theme combinations across fourteen changed chapters at 2560, 1440, and 390 pixels, with an opening and lab screenshot for each. No page errors, horizontal document overflow, missing visible images, or stretched aspect ratios. Reviewed the composed artwork and both neural workbenches in desktop and phone views.
- The production gallery loads all 65 candidates in both themes at desktop and phone sizes. Selection markers identify thirteen choices, including all six user choices. Thumbnail frames remain compact. Touch image controls are visible 44 × 44 targets; enlargement, zoom, fit, and close all passed.
- The numerical and interaction suite has 70 passing tests across sixteen files. Svelte checking and changed-component autofixers report no errors, warnings, or suggestions. The production build includes both JaxJS workers; the existing lazy tokenizer chunk-size advisory remains.
- OpenAI transport and key handling were unchanged in this pass; live API checks above belong to the earlier expansion. `.env` remains ignored.

## Key handling

Six local middleware checks passed: missing app header and foreign origin were rejected; unsupported routes and tool types were rejected; a null payload returned a validation error; local availability returned success. The actual secret was compared against 168 generated build/output files and found zero times. Only counts, never the key, were logged.

## Limits

OpenAI outputs vary by model and sampling. Vision, retrieval citations, and agent results can be wrong. The local Qwen/SmolVLM pathways were verified in the previous implementation; this expansion's new live end-to-end checks used OpenAI. The six evaluation examples are instructional, not a comprehensive benchmark. The local development proxy is not a production hosting service.

## User art curation and revised source diagrams — September 11

- Applied the latest explicit choices: forecasting A, retrieval A, evaluation B, tokens A, language C, and agents A. All eleven active edition-3 choices now come from the user. Earlier retained images are unchanged.
- Regenerated retrieval A so Fern and Moss each have their own connection to the corresponding answer row. Regenerated language C with a matching five-position context and five-by-five causal mask. Rejected intermediate edits with baked checkerboards, invented reference citations, and incorrect punctuation; their prompts and provenance remain recorded in `docs/art-revisions-v3.json`.
- Removed the duplicate static map from the bigger-picture chapter. Removed the rejected self-supervision illustration while keeping the real MNIST VAE workbench. Neither chapter has an empty image placeholder.
- Added five fresh self-supervision studies F–J covering hidden image details, temporal prediction, text continuation, matching views, and denoising. No candidate is automatically selected. The gallery contains seventy candidates, eleven user selection marks, and a collapsed archive for the five rejected self-supervision studies and the redundant atlas studies.
- Validated alpha in every exported candidate. Full/800-pixel active exports total twenty-two WebP files. Both selected diagrams and all new study labels were visually inspected against the exact dark and light app backgrounds. Alt text and intrinsic dimensions match the chosen assets.
- Captured and inspected affected layouts at 2560, 1440, and 390 pixels in both themes: forty-eight combinations, no browser errors, no horizontal overflow, no missing artwork, and no stretched image proportions. Gallery enlargement, closing, selection markers, and phone containment passed. Evidence is in `test-results/art-curation-v3/results.json` and the adjacent screenshots.
- Moved touch enlargement controls into the caption action row so they never cover an artwork label. Desktop keeps the small hover control. The visible touch target remains 44 × 44 pixels; opening, zooming, fitting, and closing passed for all six selected chapter images in both themes. The component autofixer reports no issues or suggestions.
- Svelte check reports zero errors and warnings; lint and the production build pass. This revision changes artwork, its copy, touch enlargement placement, and the review gallery; model-training and inference code are unchanged. The prior seventy-test numerical/interaction result still applies to those unchanged implementations.

## Shared palette, open inspection markers, and self-supervision H — September 11

- Consolidated the app's color roles in `src/routes/theme.css`. Light mode now uses a cool blue-gray `#edf0f6` canvas, neutral white cards, and darker blue/lavender accents. Dark mode keeps `#141619`. Primary actions, sliders, checkboxes, navigation states, and keyboard focus use the shared blue accent; validation and secondary concepts retain lavender, with amber for test data.
- Removed competing root palettes and updated the network, MNIST, pretraining, and atlas surfaces to inherit shared theme roles. Fixed dark plotting stages preserve their own readable data colors in both modes. Removed residual green fills from control and plot styles; MNIST digit categories retain distinct colors within the revised spectrum.
- Replaced the MNIST opaque selection tile with four thin, open corners. Network prediction/response probes and the regression probe use the same open-marker geometry. Clustering inspection no longer paints a background halo over neighboring points; ConvNet receptive-field inspection uses an unfilled outline.
- Verified the MNIST marker at the pixel level in both themes: its central 6 × 6 pixels exactly match the unselected map, while the outer bracket pixels change. Twelve successive drag positions yield twelve distinct coordinates. Sliders immediately change reconstructed pixels, and arrow keys move the probe. Evidence: `test-results/theme-pass/interactions.json`.
- Installed self-supervision illustration H by explicit user choice. Twelve active edition-3 selections now produce twenty-four full/compact WebP exports. The gallery marks H as selected; rejected studies remain archived. The caption distinguishes the book's next-word target from the lab's image-reconstruction objective. Light-mode export proofs now use `#edf0f6`.
- Captured all eighteen chapters at 1440 and 390 pixels in both themes: seventy-two layouts, no browser errors or horizontal overflow, and no residual green in the inspected rendered controls/SVG styles. Reviewed the overview sheets, MNIST workbench, network, artwork, and model dialog at full size. A follow-up light-mode contrast audit corrected pale retrieval document letters, reflection-choice labels, and adaptation status text.
- All seventy tests across sixteen numerical and interaction suites pass. Browser component tests now import the same shared theme as the application. Svelte checking reports zero errors/warnings and all changed-component autofixers report no issues or suggestions. Lint and the production build pass.
- Training equations and OpenAI transport are unchanged. This pass does not make new claims about live API responses; `.env` remains ignored.

## Interpreting learned features and complete widget themes — September 11

- Added a separate guided feature hierarchy above the ConvNet activation inspector: edges, contours, and garment parts, with nine empirically selected filters. Each shows six highest-response input patches from distinct images among the first 2,000 bundled training examples. The source image, exact receptive field, and measured response can be inspected. The saved checkpoint is explicitly identified; live training in the existing inspector remains independent. See `docs/feature-hierarchy.md` for selection and provenance.
- The new smoothing probe blends only the selected receptive field toward its original mean brightness and reruns the fixed model. All 54 displayed matches agree with browser inference. Tests verify unchanged outside pixels, preserved mean, the analytical response for a uniform first-layer patch, and interactive stage/probe behavior. In a measured trouser-leg example, smoothing reduced the unit response from 8.63 to 0.21.
- Replaced previously fixed dark plotting surfaces with distinct light/dark palette roles. Updated clustering, adaptation, regression boundaries, the trained reinforcement-learning board, ConvNet maps/controls, and MNIST thumbnails. Canvas colors now repaint when the theme changes. Source Fashion-MNIST images retain consistent grayscale data rendering, while activation and weight maps adapt to the surrounding theme.
- Inspected all eighteen chapters at 1440 and 390 pixels in both themes: seventy-two layouts, no runtime errors or horizontal overflow. Inspected the new hierarchy, trained reinforcement-learning policy, smoothed patches, and existing ConvNet activation maps at full size. Follow-up captures cover the final hierarchy at 390, 1440, and 2560 pixels in both themes. The light-mode text contrast audit found no plain-text items below 3:1 in the visible chapter views and model dialog; gradient surfaces and disabled states were inspected separately.
- All 73 tests across seventeen files pass. Svelte checking reports zero errors and warnings, changed-component autofixers report no issues or suggestions, and lint and the static production build pass. The existing lazy tokenizer chunk-size advisory remains. No model-training equations, saved weights, or OpenAI transport were changed in this pass.

## Connecting the representation to the actual network — September 11

- Replaced the three detached layer cards with an interactive diagram of the actual ConvNet. It displays every one of the 8, 16, and 24 feature maps, all 384 flattened values, and all ten output neurons. Each map contains its full spatial grid of neuron responses. The diagram traces real map-to-map connectivity, emphasizing links next to the selected feature; convolutional links summarize nine shared weights. Dense output connections are bundled on phones.
- Selecting any of the 48 maps updates its six strongest input patches, source image, receptive field, and measured response. The nine guided interpretations remain; other features are explicitly unnamed. Smoothing a patch updates the complete network and output probabilities. Every output logit is checked against inference.
- Added an exact single-neuron close-up: nine input connections at a time, signed learned weights, the contribution from all other incoming maps, bias, and ReLU. Cycling through input maps preserves the total output. Tests verify the arithmetic across all three layers, including zero-padded borders, and verify all 288 atlas matches.
- The six-step signal walkthrough highlights input, three convolutional layers, flattening, and the dense/softmax output in sequence. It is a slowed display of the actual calculated activations. Stop, keyboard feature selection, and reduced-motion behavior passed browser interaction checks.
- Inspected the connected overview and expanded neuron diagram in both themes at 390, 768, 1024, 1440, and 2560 pixels. The diagram reflows vertically on narrow containers. No browser errors, horizontal overflow, missing connection endpoints, or invalid path coordinates were found. Final screenshots allow theme transitions to settle before capture. Evidence is in `test-results/conv-network/`.
- All 75 numerical and interaction tests across seventeen files pass. Svelte checking reports zero errors and warnings; both edited components pass the autofixer without issues or suggestions. Lint and the final production build pass. Saved weights, training equations, and API transport are unchanged.

## Models, sound, and visible learning — September 11

- Replaced the editable model ID with an accessible account-backed model selector. The local server proxies the authenticated models list; the browser filters it to supported language/vision and speech families, collapses dated variants, and retains the selected model. Refresh, key-source changes, empty/error states, keyboard navigation, and a compact mobile settings entry are supported. The key remains server-side for the development connection; `.env` remains ignored.
- Added a fourth Chapter 1 example: real speech generation with narration, emphasis, and multilingual prompts, three voices, and delivery controls. GPT-4o mini TTS is the default for exact narration; GPT Audio 1.5 is also selectable when available. The recorded introduction is an actual generated WAV with provenance in `static/audio/provenance.json`. Live tests returned HTTP 200 for GPT Audio 1.5 and GPT-4o mini TTS. An end-to-end browser test generated an Onyx narration, decoded a 3.4-second clip, played it, and paused it successfully. No browser runtime errors occurred.
- Added an audio-frames token lesson with real waveform samples, adjustable 20/40/80 ms windows, a Hann-windowed frequency spectrum, and exact-length excerpt playback through Web Audio. The copy distinguishes these illustrative windows from a model's internal tokens. The image sequence now wraps in pages of 24; selecting an image patch selects the corresponding page. Both can be explored without horizontal scrolling.
- Added a read-only view of the actual MNIST variational autoencoder: 784 pixels, 128 and 32 encoder neurons, two means and two log variances, the two-dimensional map coordinate, 32 and 128 decoder neurons, and 784 outputs. Every hidden neuron is represented. Current responses follow the model checkpoint and selected map coordinate; exploring the map dims the unrelated encoder. Numerical tests verify all layer sizes and equality with the actual decoder output.
- Separated the ConvNet into Activations, Learned kernels, and Strongest inputs views. Every one of the 48 filters exposes all of its incoming 3×3 kernel slices rather than an average. Strongest-input tiles are actual receptive-field patches from the reference atlas. A categorized image library provides all 2,000 official test images. Changing filters or display mode preserves the chosen test image. The independently labeled evidence probe can still smooth a strong training-image match. Tests verify fixed-input behavior, all 520 kernel slices, and output logits after changing the input.
- Rebuilt forecasting with three synthetic scenarios (café orders, electricity demand, museum visitors), three nested linear models, editable training observations, and noise/growth/seasonality controls. Gradient descent uses only the chronological training subset. Validation/test points and their errors remain visible. A model inspector decomposes each prediction into learned level, trend, and season terms. Tests verify extrapolation, contribution sums, and complete independence from future observations. The chart recalculates its coordinate system on narrow screens instead of shrinking its labels; pointer editing uses the SVG screen transform.
- Added Hugeicons to the Transformer stages, speech, model inspection, and scenario controls; sorting bars now have six distinct blue shades. Chapter subtitles use consistent color roles. Visually inspected desktop and 390px layouts of speech, forecasting, image patches, the ConvNet, and the autoencoder in both themes. No horizontal overflow or runtime errors were found. Screenshots and live browser scripts are in `test-results/product-pass/`.
- All 81 numerical and browser tests pass across 21 files. The final changed components pass the Svelte autofixer; Svelte checking reports zero errors and warnings. Lint and the static production build pass. A client-artifact scan found no API key in any of the 27 generated text files. The pre-existing lazy tokenizer bundle-size advisory remains.

## Clustering, Q-learning, image generation, and TinyStories — September 11

- Added unique dashed Voronoi boundaries to k-means. The selected guest exposes every center distance; the model inspector shows the actual coordinate sums, membership counts, and next means. Numerical tests check that boundary midpoints are equidistant to their nearest two centers. Assignment and mean-update phases remain separate and visible.
- Added single-transition Q-learning with exactly the same update used by full episodes. The map's adjacent action controls show four actual values and an open transition locator. The inspector displays the reward, next state, old estimate, bootstrap target, and resulting Q entry. Tests verify one-entry mutation and zero terminal bootstrapping. The browser-trained 300-episode policy reaches delivery in 10 steps with return 8.65.
- Added the nineteenth chapter, Creating something new. The image studio uses account-backed OpenAI model choices and real image generation, with prompt presets, size and quality controls, six-result comparison, exact-prompt repetition, cancellation, and PNG downloads. A real GPT Image 2.5 Sunburst draft request returned HTTP 200 and a 1024×1024 PNG in approximately twelve seconds. Follow-up deterministic UI checks replayed that actual result to test repetition, selection, download filenames, and cancellation without further API spending. The real API result and separate UI-test reports are retained in `test-results/learning-pass/`.
- Added a real 32,768-parameter causal transformer trained on the original TinyStories release, using 2,097 complete training stories and 184 separate validation stories. Training, Adam state, random streams, and weights persist across runs. The UI retains its worker across language-tab changes, displays cumulative updates, saves writing samples, and exposes measured next-character probabilities and attention. The initialization sample remains available as a baseline alongside recent samples.
- Verified actual WebGPU training and asynchronous readback: held-out loss dropped from 4.565 to 3.164 after 25 updates in the runtime check; resuming began at step 26. A longer UI run reached 500 updates, continued to 518, preserved 518 across tab changes, and measured unseen loss 2.332. Generated text remains imperfect, as expected for a small character model. The independent attention inspection forward pass agrees with the training forward pass within 0.0001; all inspected attention rows sum to one and future positions are zero.
- Pretraining and adaptation now continue existing weights, and the image classifier and regression/neural labs can continue beyond their initial 200/600-epoch runs. Tests verify cumulative training against an uninterrupted reference model and ensure the loss plot remains within its viewBox. Explicit resets still clear the models. Café/Astronomy selection has a persistent check icon and an unambiguous filled state in light mode.
- Consolidated primary actions onto the same blue-to-lavender gradient material and selected options onto shared theme roles. Corrected a leftover mint/peach legend to match the actual blue/amber classifier weights. Inspected updated labs at 390 and 1440 pixels in both modes, with additional 900/2560-pixel checks for generation, clustering, reinforcement, forecasting, neural networks, and the autoencoder. Twenty-six extended layout combinations and twenty main-lab captures completed without horizontal overflow or browser errors.
- Generated three new transparent generalization illustrations with blue curves, lavender observations, and no cream tiles or orange rings. A is provisional pending the user's choice. All three are available in `/art-review/edition-4.html`; prompts and asset provenance are in `docs/art-prompts-v4.json` and `docs/art-assets-v4.json`. Inspected all active chapter art together on both theme backgrounds, preserving previously approved selections.
- All 91 numerical and browser tests pass across 24 files. Changed Svelte components pass the autofixer, Svelte checking reports no errors or warnings, and lint and the production build pass. `.env` remains ignored; the client-artifact scan found zero copies of the actual key in 34 generated text files. The pre-existing lazy tokenizer chunk-size advisory remains.

## Exact curves and visible neighborhoods — September 11, 2026

- **97 tests / 25 files passed.** Exact interpolation hits every training point; held-out target changes cannot alter weights; zero-noise interpolation recovers the quadratic. Forecast tests distinguish monotone compounding growth, monotone decay, and seasonality, verify correct learned rates, and check that held-out values never enter fitting. k-NN tests verify the true sorted distances and votes. The autoencoder worker test explicitly runs past 200 steps, then pauses and resets.
- **Browser rendering:** 56 combinations spanning the changed labs at 390, 900, 1440, and 2560 pixels, in light and dark themes, with no document overflow or controls outside their cards. Twelve focused follow-up captures checked the final curves and compact neighborhood layouts. Screenshots and machine-readable measurements are in ignored `test-results/mechanics-pass/`.
- **Actual MNIST behavior:** starts from random weights, unseen MSE 0.1136 → 0.0438 over 808 updates. Continued beyond the previous automatic limit; paused on demand. Five free-coordinate probes updated their nearest reference, and the displayed reference's 784 alpha values matched the corresponding official MNIST pixels. Hovering selected digits 0, 6, and 9; a pointer drag moved latent coordinate 1 from −0.867 to 1.677 and selected a nearby real 1. “Nearest real digit” is distinguished from the arbitrary decoded point.
- **Actual image request:** default picker selected GPT Image 2.5 · Flare; the outgoing request used `gpt-image-2.5-flare`. A draft square paper-star image completed in 9.4 seconds and rendered in the image studio. This timing is one local observation, not a speed guarantee.
- Svelte autofixer returned no issues or suggestions for all changed components and the session module. Static checks, lint, and production build passed. `.env` and all `.env.*` files remain ignored except the template.
- Optional k-NN artwork is at `/art-review/neighbor-studies/`, with prompts and curation notes in `docs/neighbor-art-studies.json`. Neither study substitutes for the live, mathematically computed diagram. Generated generalization artwork was removed from the chapter.

## Overfitting presentation and selected neighbor art — September 11, 2026

- Selected neighbor artwork **B** is integrated into the clustering chapter's **Predict a label · k-NN** view, under **How nearest neighbors work**. The guide collapses to keep the working model compact. Responsive WebP exports preserve transparency; the existing enlargement, zoom, and return-to-experiment controls remain available. The illustration is explicitly distinguished from k-means, and its distances are identified as schematic in the expanded explanation. Selection and provenance are recorded in `docs/neighbor-art-studies.json`.
- Replaced the large three-card comparison and explanatory sidebar with compact curve selectors, one responsive chart, and a single aligned strip of measured training, validation, and test errors. Retained the polynomial expression, learned-weight count, all 51 observations, shared axes, noise control, exact interpolation, and underlying-pattern overlay. The error-bar lengths share one linear scale across all three models. Every animated element now uses the same tweened frame, including axes and residual endpoints.
- Inspected final captures at **390, 900, 1440, and 2560 pixels in both themes**, plus the selected art at phone and desktop widths. No horizontal overflow or browser exceptions in 12 capture combinations. Captures and reports are in ignored `test-results/overfit-polish/`.
- Browser geometry checks found zero pixel deviation at training points for the exact interpolator; residual endpoints remained within floating-point tolerance of the plotted curve during four model transitions. Verified all split counts (11/20/20), noise-free interpolation, maximum noise, resampling, overlays, reduced motion, desktop/mobile art expansion, and the experiment link. Displayed default test MSE: overfit **1.968**, generalizing model **0.006**.
- **Nine targeted tests passed** across the numerical generalization/neighbor tests and learning-lab browser tests. Both changed Svelte components pass the autofixer without issues or suggestions. Svelte check reports zero errors/warnings; lint and the production build pass.

## Exploration that changes the experience — September 11, 2026

- Replaced the default 35-cell route with three 63-cell environments: toll bridges with three crossings, competing +4/+18 deliveries, and a harder dead-end map. Toll costs and the random seed can be changed. Layouts define only streets, terminal rewards, and costs; all Q-values begin at zero.
- Separated greedy policy evaluation from real training trips. The latter records each exploratory choice and can be watched, paused, and resumed while the actual value table updates. The exploration slider changes subsequent training actions without erasing learning. A visits view, per-action trial counts, the exact latest Q-update, net training returns, and discounted policy returns expose what was learned.
- Added fresh-agent comparisons at 0%, 40%, and 80% exploration with equal **episode** budgets of 200 or 2,000 and the same seed. Episode lengths (and therefore update counts) may differ. Selecting a result loads a clone; continued training cannot mutate the saved comparison. Fixed a stale keyed-card rendering issue found during visual review.
- **Observed default bridge results, seed 41, 200 episodes:** 0% → 20 moves / discounted return 4.92; 40% → 14 moves / 7.01; 80% → 8 moves / 8.96. Seeds 42 and 43 also produced those route lengths. In the competing-delivery world after 2,000 episodes, 80% discovered a policy to B; 0% and 40% retained A. The dead-end world remained harder; finding a terminal a few times does not imply the start-state policy has learned to reach it.
- **26 targeted tests passed across five files.** Checks include reachable terminal states, exact local and terminal Q-updates, discounted returns, actual epsilon-controlled choices, route differences, toll-dependent crossings, snapshot/RNG isolation, rendered comparison results, continued training, watched-trip pause/resume, map reset, route-overlay alignment, and mobile overflow.
- Inspected the final bridge chapter at 390, 900, 1440, and 2560 pixels in light and dark themes, plus both other maps. No browser exceptions or document overflow. Exploration and training controls sit above the map at all sizes; choosing a comparison scrolls its route into view when needed. Captures and measurements are in ignored `test-results/rl-pass/`.
- Svelte autofixer returns no issues or suggestions. Svelte check reports zero errors/warnings. Lint and production build pass.

## Six problems for the neural workbench — September 11, 2026

- Added a compact, responsive dataset gallery: clouds, XOR, rings, moons, checkerboard, and intertwined spirals. Each preview comes from the same synthetic generator as its experiment. New problems contain 192 training, 64 validation, and 64 test examples; the original ring retains its 96/32/32 split. Every split is balanced and has distinct coordinates. Resampling changes the seeded observations.
- Dataset changes preserve the selected architecture and input controls, stop training, and reset model weights and history. All class labels, probability readouts, and examples follow the selected problem. Added an interpolated decision contour and smaller markers for the denser datasets. The actual network, neuron arithmetic, and independently visible held-out examples remain connected.
- Neural classification now trains until paused, with genuine continuation, bounded loss-history storage, and automatic pausing when the document is hidden or the component is destroyed. Other training labs retain their existing behavior.
- Numerical checks at the default seed show the intended capacity differences: clouds reach 100% held-out accuracy with no hidden layer; XOR reaches 48% with a line and 100% with four hidden neurons. At 600 epochs, four-neuron networks reach 81% on checkerboard and 75% on spirals, while two layers of twelve reach 100% on both. These are observed results on the synthetic held-out sets, not promises for other seeds or architectures.
- Ran all six experiments through the actual browser UI beyond the former 600-epoch cutoff. Each reached 100% test accuracy with the default two-layer architecture. Inspected trained boundaries and final layouts at 390, 900, 1440, and 2560 pixels in both themes; no document overflow or browser exceptions. Captures and measurements are in ignored `test-results/neural-datasets/`.
- **38 targeted tests passed across five files**, including dataset generation, capacity differences, exact neuron arithmetic, dataset switching during learning, resampling, mobile controls, training past the old cutoff, and pause/continue. Both changed components pass the Svelte autofixer without suggestions. Svelte check reports zero errors/warnings; lint and production build pass.

## Tool-calling chapter — September 13, 2026

- Added chapter 18, **Give the model a calculator**, at `#tool-calling`, before the agents lesson. Pattern now has twenty chapters.
- `npm run check`, `npm run lint`, and `npm run build` pass. The complete unit/browser suite passes 136 tests across 31 files, including thirteen new calculator tests.
- With the local OpenAI connection and `gpt-4.1-mini`, compared the identical question `73829461509382746159 × 49281736058472916387` in fresh contexts with and without `multiply_integers`.
- The unaided model returned `3636839279279279279279279279279279273` (incorrect). The assisted model requested the exact operands as strings, the app returned `3638444035444585948898224156135652407533`, and the model returned that exact product. This is an observed run, not a guaranteed outcome or a canned answer.
- Unit tests verify known 8×9, 20×20, 40×40, and 80×80-digit products, signed inputs and zero, rejection of floating-point JSON arguments and malformed requests, strict answer verification, actual tool-result handoff with call IDs and native reasoning items, missing/mistaken tool calls, bounded loops, and cancellation before execution.
- Browser tests cover clearing stale results after editing, calculator-only use without a model, honest handling of missing tool calls and incorrect final answers, cancellation and retry, and mobile width in both themes.
- Inspected the complete chapter on desktop and 390-pixel mobile in dark and light themes. The live run had no page errors and no mobile horizontal overflow. The calculator uses the shared provider/session; a live local WebGPU model was not loaded for this chapter’s verification.

### Transparency and parallel comparison

- Both initial OpenAI requests were observed starting at the same millisecond, before either response began. The tool-assisted follow-up waited for the calculator result while the independent baseline ran concurrently. GPT-4.1 mini again returned an incorrect unaided product and the exact tool-assisted product.
- The UI shows the shared system/user prompts and provider-specific function schema, then preserves every model turn, raw function-call output, call ID, and exact tool-return content. Expandable inspectors contain the real request body and native response output. Local runs preserve the raw generated text alongside parsed tool calls.
- The full suite passes 141 tests across 32 files. New checks exercise concurrent request dispatch, fixed model configuration, cancellation of both requests, session locking until both settle, independent local-worker instances, local preparation failure, preservation of intermediate text, visible tool definitions, and mobile transcript overflow.
- Type checking, lint, Svelte autofixer, and the static production build pass. The live production-preview comparison had no page errors; desktop and 390-pixel mobile layouts were inspected in dark and light themes. Live local GPU inference was not repeated; the independent-worker path was tested with controlled model stubs.

## GitHub Pages deployment — September 13, 2026

- Added an Actions workflow for pushes to `main` and manual runs. It uses Node.js 24, `npm ci`, Chromium browser tests, type checks, lint, the complete unit/browser suite, and a static build before deployment to GitHub Pages.
- The build reads `BASE_PATH` from the Pages configuration. Fixed bundled image, image-srcset, audio, Fashion-MNIST, and TinyStories URLs to respect that path; normal local development remains at `/`.
- All **141 tests across 32 files** pass. Svelte checking reports zero errors/warnings, lint passes, and the changed Svelte components pass the autofixer without issues or suggestions.
- Built with `BASE_PATH=/pattern` and served only the resulting static files. Browser verification visited all twenty chapters, loaded the real datasets and audio under `/pattern/`, exercised image/caption tabs and TinyStories loading, and verified the calculator's exact result. No browser exceptions, failed asset responses, or requests for assets outside the base path. The calculator chapter had no horizontal overflow at 390 pixels in either theme.
- Scanned publishable source and the static build for the actual local OpenAI key; no copies were found. The Pages workflow requires no OpenAI secret.
- Live training tests allow up to 60 seconds for the required epoch counts on shared CI CPUs; epoch, pause/resume, reset, and weight-preservation assertions are unchanged.
